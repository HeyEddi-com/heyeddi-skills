# Vocabulary: HeyEddi CI guide

- **authorize merge**: explicit current-turn phrase required before `gh pr merge`
- **Living contract**: `GET /api/public/eddi-ci-policy` via `load_policy_contract`
- **Review marker**: `<!-- heyeddi-ci-review -->`
- **Debate**: `/heyeddi ask` / reply on HeyEddi finding; advisory only
- **Spot runners**: GHA overflow/always and sealed `pipeline:` — shipped; evidence before claims
- **Ephemeral artifacts**: `.heyeddi/docs/pr-*` session scratch; never commit
