---
name: heyeddi-ci-respond
description: "Deprecated alias — use @heyeddi-pr-respond for all PR review responses including HeyEddi CI root summaries and inline findings."
version: 2.0.0
product-version: 3.4.3
author: HeyEddi-com
metadata:
  internal: true
  alias_of: heyeddi-pr-respond
disable-model-invocation: true
---

# Deprecated: use `@heyeddi-pr-respond`

**Merged into `@heyeddi-pr-respond` (v2.0.0).** Human review, HeyEddi CI root summaries, and inline threads are one pipeline.

Read and follow **`heyeddi-pr-respond/SKILL.md`** — same tools, no separate CI respond flow.

Do not write `pr-*-ci-*` or `pr-*` scratch files under `.heyeddi/docs/`.
