{
  "skill": "pre-merge-gate",
  "version": "1.1.0",
  "tools": [
    {
      "name": "run_pre_merge_gate",
      "description": "Orchestrate pre-merge checks (backend+frontend). Exit 1 on any FAIL.",
      "parameters": {
        "type": "object",
        "properties": {
          "project_root": {
            "type": "string",
            "description": "Workspace root path"
          }
        },
        "required": [
          "project_root"
        ]
      },
      "script": "scripts/pre_merge_gate.py",
      "readonly": true
    }
  ]
}
